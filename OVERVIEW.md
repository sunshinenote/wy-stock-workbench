# 股票复盘工作台 · 交付概览

## 交付内容（F:\A_WorkBuddy\stock-workbench\）

| 文件 | 作用 |
|---|---|
| `index.html` | 单文件工作台（零外部依赖），每日复盘 + 未来大事前瞻 |
| `collect.py` | 多源容错数据采集脚本（AKShare + 腾讯直连） |
| `.github/workflows/collect.yml` | 每个交易日 15:10 自动采集并更新 |
| `data/watchlist.txt` | 自选股池（公开数据，10 只示例） |
| `data/*.json` | 真实采集数据（2026-08-08 已跑通） |
| `README.md` | 本地使用 + GitHub Pages 部署手册 |

## 关键决策

1. **架构**：云端采集（GitHub Actions）+ 静态展示。个人持仓成本/笔记存浏览器 localStorage，不进仓库，隐私安全。
2. **数据源实测选型**（2026-08-08 实测）：
   - 指数/自选/历史K线 → 腾讯接口（稳定，直连）
   - 板块涨幅榜 → 同花顺 `stock_board_industry_summary_ths()`
   - 财经日历（CPI/PPI/美联储等）→ 百度股市通 `news_economic_baidu()`
   - 解禁 → 东财 `stock_restricted_release_detail_em()`
   - 业绩预告 → 东财 `stock_yjyg_em()`
3. **踩坑记录**：东财"全市场快照"接口（5000+ 只股票）易被限流断连，改用腾讯批量接口一次请求解决；akshare 多个接口参数 2026 年已变更（`tool_economic_calendar` 已移除、同花顺行业接口退化）。
4. **前瞻优先**：页面布局为「今天要处理 → 未来大事前瞻 → 每日复盘」，前瞻含倒计时预警 + 自选解禁联动提醒。

## 已验证

- [x] collect.py 8 个模块全部采集成功（真实数据）
- [x] 指数/板块/日历/解禁/自选/业绩/历史数据内容校验通过
- [x] 本地 HTTP 服务页面与数据均 200 可达
- [x] 内联 JS 语法检查通过

## 后续可迭代

- 自选股持仓盈亏自动关联（成本录入已内置）
- 添加北向资金、龙虎榜模块（东财接口，注意限流重试）
- 每季度财报季后自动生成"财报日历"
- 接入用户通达信指标信号（pytdx 读取本地自选）

## 隐私提示

部署到 GitHub Pages 后链接公网可访问；watchlist.txt 随仓库公开，勿放敏感信息；个人数据只在浏览器本地。
